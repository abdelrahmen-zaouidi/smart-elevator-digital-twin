apiVersion: 1

providers:
  - name: ElevatorOS
    orgId: 1
    folder: ElevatorOS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
